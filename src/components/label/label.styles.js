import { StyleSheet } from "react-native";
import { colors } from "../../constants/colors";

export const styles = StyleSheet.create({
  // container: {
  //   marginTop: 20,
  // },
  label: {
    fontSize: 17,
    color: colors.white,
    // fontFamily: "Lato-Bold",
    fontWeight: "400", // TODO replace for fontFamily: "Lato-Bold",
  },
});
